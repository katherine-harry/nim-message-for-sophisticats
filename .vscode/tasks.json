{
    "taskName": "Run client.nim",
    "command": "nim",
    "args": ["c", "-r", "--colors:on", "--hints:off", "client.nim"],
    "options": {
        "cwd": "${workspaceRoot}"
    },
    "type": "shell",
    "group": {
        "kind": "build",
        "isDefault": true
    }
}
